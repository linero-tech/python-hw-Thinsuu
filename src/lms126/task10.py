from to_do import TODO


def task10():
    return """
    Go to the browser and type the page name or keyword to 
    create an email
    Click the create an account
    Enter first name and last name
    Set desirable account address
    IF the account address is taken or invalid THEN it will be 
    notified and given  suggested address ELSE the user can 
    fill in until the account address is valid and new
    Set the password with more than six characters as 
    the requirement
    IF the password length is less than 6 characters <6 THEN 
    reset until it is valid ELSE confirm the password by 
    entering the same password
    Click Next
    """
